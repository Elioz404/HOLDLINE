node_modules/
dist/
.env
.env.local
*.tgz

# Probe output can contain unmasked transcripts from real calls. Never commit it.
probe-output/

# Intermediate screencast. The encoded mp4 beside it is the deliverable.
docs/video/holdline-raw.webm
docs/video/holdline-silent.mp4
docs/video/narration.wav
docs/video/audio/

# Rendered video output. The pipeline that makes it is the source; the render
# is not, and one of its segments was built from real calls.
docs/video/
